# This workflow will run tests using node and then publish a package to GitHub Packages when a release is created
# For more information see: https://docs.github.com/en/actions/publishing-packages/publishing-nodejs-packages

name: Publish Less Player Desktop

on:
  release:
    types: [created]

jobs:
  publish:
    runs-on: ${{matrix.os}}
    strategy:
      matrix:
        os: [windows-latest, macos-12.6]
    permissions:
      contents: write
      packages: write
    steps:
      - uses: actions/checkout@v3
      - uses: actions/setup-node@v3
        with:
          node-version: 18
          registry-url: https://npm.pkg.github.com/
      - run: npm install
      - run: npm run dist
        env:
          NODE_AUTH_TOKEN: ${{secrets.GITHUB_TOKEN}}
          GH_TOKEN: ${{secrets.GITHUB_TOKEN}}
      
      - name: Clean Windows output 
        if: ${{ matrix.os.contains('win') }}
        run: | 
          npx rimraf "output/!(*.exe)"
      
      - name: Clean macOS output 
        if: ${{ matrix.os.contains('mac') }}
        run: | 
          npx rimraf "output/!(*.dmg)"
          
      - name: Release
        uses: softprops/action-gh-release@v1
        if: startsWith(github.ref, 'refs/tags/')
        with:
          files: "output/**"
          token: ${{ secrets.GITHUB_TOKEN }}
        env:
          GITHUB_TOKEN: ${{secrets.GITHUB_TOKEN}}
          GITHUB_REPOSITORY: GeekLee2012/Less-Player-Desktop
          
